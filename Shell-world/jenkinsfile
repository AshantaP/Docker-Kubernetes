pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Running shell script"
                    '''
                    sh hello.sh
                    '''
            }
        }
    }
}
